# このアプリ（bbh-rsvp）の秘密情報ルール

- コミットする前に、必ず差分（変更内容）を確認する。
- APIキー・パスワード・トークン・秘密鍵・実際の回答者名やメールアドレスなどの実データは、理由を問わずGitに含めない。
- `.env` や `.dev.vars` など秘密を含むファイルは `.gitignore` で除外し、`git status` で追跡対象に紛れ込んでいないか都度確認する。
- コミット前チェック（`.git/hooks/pre-commit`）が秘密情報らしき文字列を検知したら、コミットを止めて内容を確認する。誤検知だと確認できた場合のみ、内容を再確認した上で `--no-verify` を使う。
- 秘密が誤って履歴に入ったことがわかった場合は、値を表示せずに対象ファイル・キー名だけ報告し、該当キーの無効化・再発行を先に行う。
